import { makeStyles } from '@material-ui/core';

export const useUpdateAddressPanelStyles = makeStyles(() => ({
  headerShadow: {
    boxShadow: '0 4px 4px 0 rgba(12,0,51,0.1)',
  },
  padding: {
    padding: '1rem',
  },
}));
